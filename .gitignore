.DS_Store
.config/
/coverage/
.worktrees/
node_modules/

# Fuseki runtime state (databases, logs, backups — local-only)
/run/

# Claude Code per-developer settings (skills under .claude/skills/ are tracked)
.claude/settings.local.json
.claude/settings.local.*

# Private specs — not candidates for open-source delivery (move to a private location)
/docs/superpowers/specs/*aspice-aaki*

# Draft pending review/placement — keep untracked for now
/docs/AAKI-and-the-Digital-Thread.md

# Workspace member packages — each is its own git repo under the OSLC org,
# cloned here for npm-workspace development. The parent oslc4js repo is the
# umbrella (docs, workspace config, .idea); package source is owned by its
# own repo, not double-tracked here.
/bmm-server/
/fs-storage-service/
/github-server/
/jena-storage-service/
/ldp-app/
/ldp-service/
/mongodb-storage-service/
/mrm-server/
/oslc-browser/
/oslc-client/
/oslc-mcp-server/
/oslc-server/
/oslc-service/
/storage-service/
